# Changelog Archive — GAS Template (Google Apps Script)

Older version sections rotated from [GasTemplategs.changelog.md](GasTemplategs.changelog.md). Full granularity preserved — entries are moved here verbatim when the main changelog exceeds 50 version sections.

## Rotation Logic

When the main changelog exceeds 50 `## [XX.XXg]` version sections:
1. Find the **oldest date** among all version sections
2. Move ALL sections from that date to the archive (prepend — newest archive entries at top)
3. Recount sections — if still >50, repeat from step 1
4. Update the `Sections: X/50` counter in the main changelog

*(No archived sections yet)*

Developed by: ShadowAISolutions
